# MongoDB Atlas Connection String
MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority
DATABASE_NAME=mymonitorxx

# Firebase Admin SDK Credentials JSON path (or leave empty for dev mode / mock token bypass)
FIREBASE_CREDENTIALS_PATH=firebase_credentials.json

# App Environment (development / production)
ENVIRONMENT=development
DEV_MODE=true

# Allowed CORS Origins (comma separated)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000,http://localhost:5500,http://127.0.0.1:5500,https://canvaonly322.github.io

# Campus Location Configuration (Defaults)
CAMPUS_NAME=Central Engineering Campus
DEFAULT_LATITUDE=12.9716
DEFAULT_LONGITUDE=77.5946
DEFAULT_CHECKIN_RADIUS_METERS=60.0
